# Environment variables (never commit these)
.env
.env.local
.env.*.local

# Dependencies
node_modules

# Build output (deployed via gh-pages, not committed to main)
dist

# Editor / OS
.DS_Store
Thumbs.db
*.log